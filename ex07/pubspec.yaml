name: ex07
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  image_picker: ^1.0.4
  flutter_lints: ^6.0.0
  mask_text_input_formatter: ^2.9.0

flutter:
  uses-material-design: true

  assets:
    - assets/imagem/
